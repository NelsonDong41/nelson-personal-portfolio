import styled from "@emotion/styled";
import { Box, Paper } from "@mui/material";

const StyledExperienceCard = styled(Paper)`
  padding: 5%;
`;

const ExperienceContentCard: React.FC = () => {
  return (
    <StyledExperienceCard>
      
    </StyledExperienceCard>
  );
};

export default ExperienceContentCard;
